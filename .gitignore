__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# ข้อมูลลับ/ไฟล์ดาวน์โหลดชั่วคราว — ห้าม commit เด็ดขาด
.env
*.env
amr_downloads/
amr_selenium.log
amr_selenium_progress.json
amr_selenium_failed.log
debug_popup_*.html
debug_showperiod_*.html
debug_noPopup_*.html

# ทะเบียนผู้ใช้ไฟจริงสำหรับดูในเครื่องตัวเองเท่านั้น — ห้าม commit เด็ดขาด
# (ดู data/reference/customers_local.csv.example สำหรับรูปแบบไฟล์)
data/reference/customers_local.csv

# ประวัติการนำเข้า AMR จริง (มีชื่อบริษัท/เลขบัญชีจริง) สำหรับดูในเครื่องตัวเองเท่านั้น
# — ห้าม commit เด็ดขาด (เขียนอัตโนมัติโดย amr_import.import_amr_auto ทุกครั้งที่นำเข้าสำเร็จ)
data/reference/import_log_local.csv

# เส้นโค้งรายชั่วโมงของแต่ละไซต์/บัญชีจริงแยกต่างหาก (มีชื่อบริษัท/เลขบัญชีจริง) สำหรับดูใน
# เครื่องตัวเองเท่านั้น — ห้าม commit เด็ดขาด (หลักการเดียวกับ import_log_local.csv) เขียน
# อัตโนมัติโดย amr_import.import_amr_auto ทุกครั้งที่นำเข้าสำเร็จ ต่างจาก load_curves.csv ซึ่ง
# เป็นค่าเฉลี่ยรวมของทุกไซต์แบบ anonymized ที่ commit เข้า repo ได้
data/reference/site_curves_local.csv

# ฐานข้อมูล SQLite ที่ดึงมาจาก DBD Open Data (dbd_opendata.py) — ไฟล์ใหญ่/เป็นข้อมูลที่ดึงมาเอง
# ในเครื่อง (reproducible ได้ด้วยการรันดึงใหม่) ไม่ใช่ source data ของโปรเจกต์ ไม่ควร commit
data/reference/dbd_juristic_local.db

# รายการ AMR ที่นำเข้าไม่สำเร็จเพราะไม่ทราบประเภทธุรกิจ/รหัสอัตรา (มีชื่อบริษัท/เลขบัญชีจริง)
# รอกรอกภายหลังในหน้า Admin ส่วน "รอทราบอัตรา" — หลักการเดียวกับ import_log_local.csv
# ห้าม commit เด็ดขาด
data/reference/pending_amr_local.csv
